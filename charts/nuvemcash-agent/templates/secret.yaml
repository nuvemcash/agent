{{- if not .Values.connection.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-token
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  token: {{ required "connection.token (ou connection.existingSecret) é obrigatório" .Values.connection.token | quote }}
{{- end }}
